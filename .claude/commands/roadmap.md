Read ROADMAP.md and report the current state of work.

Output:

1. **Phase and goal** — current phase and what the milestone target is
2. **In progress** — any items currently marked `in-progress`, who is working on them if known
3. **Open (high priority)** — all `open` items with `priority: high`, sorted by dependencies (unblocked first)
4. **Open (medium/low)** — brief list only, no details
5. **Blocked** — any items with `status: blocked` and what they are blocked on
6. **Recommendation** — which item to pick up next and why (consider priority, dependencies, and effort)

If ROADMAP.md is empty or only contains template content, say so and suggest running the setup interview first.
